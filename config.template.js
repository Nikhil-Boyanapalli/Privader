/**
 * Configuration Template for Privader Web Security
 * 
 * Instructions:
 * 1. Rename this file to config.js
 * 2. Replace the placeholder API key with your own
 * 3. Never commit your config.js file to version control
 */

const config = {
    /**
     * Google Safe Browsing API Key
     * To get your API key:
     * 1. Go to Google Cloud Console (https://console.cloud.google.com)
     * 2. Create a new project or select an existing one
     * 3. Enable the Safe Browsing API
     * 4. Go to Credentials and create an API key
     * 5. Copy your API key and paste it below
     * 
     * Note: The free tier includes:
     * - Up to 10,000 requests per day
     * - No credit card required
     * - Suitable for personal and small business use
     */
    SAFE_BROWSING_API_KEY: 'YOUR_API_KEY_HERE'
};

export default config; 